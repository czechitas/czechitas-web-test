---
layout: default.njk
pagination:
    data: courses.other
    size: 1
    addAllPagesToCollections: true
    alias: item
permalink: "kurzy/{{ [item.Code, ': ', item.Name] | join | slugify }}/index.html"
eleventyComputed:
  title: "{{ item.Name }}"
---

{{ item | dump | safe }}
